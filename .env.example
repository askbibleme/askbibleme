# 复制为 .env 后填写（.env 已被 .gitignore 忽略，不会进仓库）
# 本地开发需要调用 OpenAI 时设置其一即可：
# OPENAI_API_KEY=sk-...
#
# 线上环境建议在服务器配置环境变量，或使用后台保存到 admin_data/system_secrets.json
# （该文件已加入 .gitignore，勿手工把含真实 Key 的文件提交到 Git）

OPENAI_API_KEY=
