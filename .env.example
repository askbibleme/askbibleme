# 复制为 .env 后填写（.env 已被 .gitignore 忽略，不会进仓库）
# 本地开发需要调用 OpenAI 时设置其一即可：
# OPENAI_API_KEY=sk-...
#
# 读取优先级：环境变量 OPENAI_API_KEY 优先于后台「系统密钥」（admin_data/system_secrets.json）。
# 若批量任务 401 invalid_api_key：在 OpenAI 控制台换新 Key 后，改此处或后台保存其一即可。
# （system_secrets.json 已 .gitignore，勿把含真实 Key 的文件提交 Git）

OPENAI_API_KEY=

# 可选：人像 + 章节仿古插画等创作侧运行数据根目录（持久卷；与留言同级，不被代码发布覆盖）。
# 设置后以下文件均在此目录，不再读写 admin_data 下同名文件：
# character_illustration_profiles.json、character_profile_image_audit.json、
# character_stage_rules.json、chapter_key_people.json、chapter_illustration_states.json
# CHARACTER_DATA_DIR=/var/lib/askbible/creative_runtime_data
#
# 可选：人物档案改用 SQLite（大表，仍在发布目录外）。可与 CHARACTER_DATA_DIR 同目录，例如：
# CHARACTER_PROFILES_DB=/var/lib/askbible/creative_runtime_data/character_profiles.sqlite
# 不设 CHARACTER_PROFILES_DB 时档案为 JSON，路径在 CHARACTER_DATA_DIR 或 admin_data。
# 可选：SQLite 空库时导入的种子（默认仓库内 admin_data/character_illustration_profiles.json）
# CHARACTER_PROFILES_SEED_JSON=

# 可选：整块 admin_data 与 auth 等迁到持久卷时设置（见 server.js DATA_ROOT）
# DATA_ROOT=

# 可选：命令行一键部署（npm run deploy:remote -- deploy-builds/xxx.zip）
# ASKBIBLE_DEPLOY_URL=https://你的域名
# ASKBIBLE_ADMIN_TOKEN=与浏览器后台请求里 Authorization: Bearer 后面相同（勿提交真实 token）
